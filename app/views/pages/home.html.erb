
  <div class="banner mb-3" style="background-color: #007DA6;">
    <h1>Votre Super Héro favoris</h1>
    <h1>à côté de chez vous !</h1>
    <p class="formulaire"><%= form_with model: @heroe do |f| %>
  <% f.label :address %>
  <%= f.text_field :address, placeholder: "Votre ville" %>
  <%= f.submit class: "button" %>
<% end %></p>
  </div>
  <div class="container">
      <div class="cards">
          <% @heroes.sample(3).each do |heroe| %>
          <div class="card">
          <div class="heroes-cards"><%= heroe.photos.attached? ? cl_image_tag(heroe.photos.first&.key, alt: "#{heroe.name}", class:"movie-image") : image_tag("superhero.jpg")%>
            <div><p class="h2"><%= heroe.name %></p><br>
            <p class="fs-6 p-3"><%= heroe.description %></p>
            </div>
              </div>
            <button class="button">Montrer</button>
            </div>
        <% end %>
        </div>
    </div>
</div>
